- var user = { descriptionX: 'foo bar baz' }
- var authorised = true
#user
  if user.description
    h2.green Description
    p.description= user.description
  else if authorised
    h2.blue Description
    p.description.
      User has no description,
      why not add one...
  else
    h2.red Description
    p.description User has no description
//- <div id="user"><h2 class="blue">Description</h2><p class="description">User has no description,\nwhy not add one...</p></div>
